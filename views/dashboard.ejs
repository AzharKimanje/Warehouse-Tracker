<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warehouse Dashboard</title>
</head>
<body>
    <h1>Warehouse Dashboard</h1>
    <p>Welcome to the dashboard!</p>

    <h2>Items List</h2>
    <ul>
        <% items.forEach(item => { %>
            <li>
                <strong><%= item.name %></strong> - Brought by: <%= item.broughtBy %><br>
                Location: <%= item.location %><br>
                Barcode: <img src="data:image/png;base64,<%= item.barcode %>" alt="Barcode"><br>
            </li>
        <% }) %>
    </ul>

    <a href="/auth/login">Log out</a>
</body>
</html>
